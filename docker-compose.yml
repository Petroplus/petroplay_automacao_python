version: "3.9"

services:
  backend:
    build: .
    container_name: petroplus_api_python_container
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    restart: always
